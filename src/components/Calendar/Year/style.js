import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
  },
  label: {
    backgroundColor: '#aaa',
    padding: 15
  },
  summary: {
    backgroundColor: '#dedede',
    padding: 15
  }
});
