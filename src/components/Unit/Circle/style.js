import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  circle: {
    borderRadius: 60,
    width: 60,
    height: 60,
    backgroundColor: 'red',
    alignItems: 'center',
    marginBottom: 5
  }
});
